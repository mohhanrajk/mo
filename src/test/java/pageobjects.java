
public class pageobjects 
{
	//declare all colors in static variable
	
	
	public static void testsdjk()
	
	{
		switch 2 : test4();
					break;
	}
	
	
	
	
	public static void test4()
	{
		
		//driver method name pass paramer in xpath . get color 
		boolean t = true;
		if(t=true)// check passed value is enabled or already selected//create boolean function //"passed element is enabled or disabled or already selected"
		{
			if(t)
			{
				//click selected option
				//compare 
				
			}
			else if(t)  //alreadyselected
			{
				
			}
			
		}
		else if(t)
		{
			
		}
		
			
		}




	int enable method(xpath)
	{
		boolean t  = driver.element.method is EnabledAssertion();
		int k = gettext of number and convert into number
				
		if(t)
		{
			background color and font color
			
			//it is ready to clickable that is not selected
			return1;
		}
		else
			if(t=<0)
			{
				background color and font color				//link should be disabled
				return 2
			}
			else
			{
				background color and font color				//link already selected
				return 3
			}
	}

	
}
